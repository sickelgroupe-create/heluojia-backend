CREATE TABLE IF NOT EXISTS shop_catalog_import(ticket VARCHAR(50) PRIMARY KEY,owner_name VARCHAR(80) NOT NULL,rows_json LONGTEXT NOT NULL,result_json LONGTEXT NULL,status VARCHAR(20) NOT NULL DEFAULT 'ready',expires_at DATETIME NOT NULL,created_at DATETIME DEFAULT CURRENT_TIMESTAMP);
