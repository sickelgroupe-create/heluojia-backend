# 交付验证记录 · 2026-09-21

- 后台与小程序均从锁文件重新安装依赖；两端生产构建通过，小程序不依赖 HBuilderX 本机路径。
- H5、微信小程序、后台和后端 JAR 构建通过。
- 新建空库顺序导入三份 SQL 成功，拒绝向非空数据库重复导入。
- 完整 112 张表启动成功；管理员设置新密码后可登录，商城管理路由正常。
- 七款商品固定零售价一致；18 个公共图片请求成功，内容 SHA-256 与交付文件一致。
- 24 份启动 SQL 已打入 JAR，并逐份核对内容一致。
- 后端测试 44 项：43 项通过，1 项需要外部证书配置的可选测试跳过。包含本地数据库的优惠券删除、购买方式、订单流转、支付退款回归。
- 测试均在本机隔离数据库执行；本次整理未进行真实扣款。微信实测沿用此前发布版本的验收，不代表新服务器免配即可支付。
- 新目录扫描未发现现网密码、AppSecret、APIv3 密钥或私钥文件。

空库测试修正了退款测试数据长度：测试生成的微信退款单号不再依赖旧数据库已增长的自增编号。业务支付逻辑未因此改变。

结构化结果见 `verification.json`。`source-manifest.json` 用于核对交付源文件；后续有意修改代码后，可重新执行 `python deploy/check_handoff.py --write-manifest`。
